
Call Stack (Стек вызовов) -
Это место, где выполняется **синхронный** JavaScript-код.  
Каждый вызов функции попадает в стек, выполняется и удаляется оттуда.  
Пока стек не пуст — **новый код не может начать выполняться.**

Когда вызываешь что-то асинхронное (например, `setTimeout`, `fetch`, `fs.readFile`),  
эта задача **уходит из стека** в **среду выполнения (Web API)**, где работает "в фоне".
### 🔹 3. **Callback Queue (Очередь обратных вызовов / макрозадачи)**

Когда стек освободился, Event Loop берёт первую задачу из этой очереди и помещает её в стек.

 
### **Microtask Queue (Очередь микрозадач)**

Это отдельная очередь для **промисов** и некоторых других механизмов (например, `queueMicrotask` или `process.nextTick` в Node.js).